name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test:
    name: Test ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-24.04
            platform: linux-x86_64
          - os: macos-15
            platform: darwin-aarch64
          - os: windows-2025
            platform: windows-x86_64
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15

    steps:
      - name: Check out the tested commit
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Install MoonBit
        if: runner.os != 'Windows'
        shell: bash
        run: |
          curl --proto '=https' --tlsv1.2 -fsSL https://cli.moonbitlang.com/install/unix.sh | bash
          echo "$HOME/.moon/bin" >> "$GITHUB_PATH"

      - name: Install MoonBit
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
          Invoke-RestMethod https://cli.moonbitlang.com/install/powershell.ps1 | Invoke-Expression
          Join-Path $env:USERPROFILE '.moon\bin' | Out-File -FilePath $env:GITHUB_PATH -Append

      - name: Configure MSVC
        if: runner.os == 'Windows'
        uses: ilammy/msvc-dev-cmd@0b201ec74fa43914dc39ae48a89fd1d8cb592756 # v1.13.0

      - name: Record the toolchain
        run: moon version --all

      - name: Update the Mooncakes registry
        run: moon update

      - name: Resolve dependencies and check native packages
        run: moon check --target native --deny-warn

      - name: Run release tests
        run: moon test --target native --release --frozen

      - name: Build stripped release binary
        run: moon build --target native --release --strip --frozen cmd/pumd

      - name: Locate the Unix binary
        if: runner.os != 'Windows'
        shell: bash
        run: |
          binary_base="_build/native/release/build/rainbowdashy/pumd/cmd/pumd/pumd"
          for candidate in "$binary_base" "$binary_base.exe"; do
            if [[ -f "$candidate" ]]; then
              echo "PUMD_BINARY=$candidate" >> "$GITHUB_ENV"
              exit 0
            fi
          done
          printf 'native pumd binary was not found under %s\n' "$(dirname "$binary_base")" >&2
          find "$(dirname "$binary_base")" -maxdepth 1 -type f -print >&2
          exit 1

      - name: Locate the Windows binary
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $binaryBase = '_build/native/release/build/rainbowdashy/pumd/cmd/pumd/pumd'
          $binary = @($binaryBase, "$binaryBase.exe") | Where-Object { Test-Path -LiteralPath $_ -PathType Leaf } | Select-Object -First 1
          if (-not $binary) {
            throw "native pumd binary was not found under $(Split-Path $binaryBase)"
          }
          "PUMD_BINARY=$binary" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Smoke test the Unix binary
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set +e
          output="$("$PUMD_BINARY" 2>&1)"
          status=$?
          set -e
          if [[ "$status" -ne 1 ]]; then
            printf 'expected pumd without arguments to exit 1, got %s\n%s\n' "$status" "$output" >&2
            exit 1
          fi
          if [[ "$output" != *"Project Authorization usage:"* ]]; then
            printf 'pumd did not print its usage marker:\n%s\n' "$output" >&2
            exit 1
          fi

      - name: Smoke test the Windows binary
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $PSNativeCommandUseErrorActionPreference = $false
          $output = & $env:PUMD_BINARY 2>&1 | Out-String
          if ($LASTEXITCODE -ne 1) {
            throw "expected pumd without arguments to exit 1, got $LASTEXITCODE"
          }
          if ($output -notmatch 'Project Authorization usage:') {
            throw "pumd did not print its usage marker: $output"
          }
          exit 0

      - name: Prove Unix native-binary preflight is vault-free
        if: runner.os != 'Windows'
        shell: bash
        run: |
          source_file="$(mktemp)"
          trap 'rm -f "$source_file"' EXIT
          printf '![unsupported](image.png)\n' > "$source_file"
          set +e
          output="$("$PUMD_BINARY" publish "$source_file" 2>&1)"
          status=$?
          set -e
          if [[ "$status" -ne 1 || "$output" != *"rendering failed"* ]]; then
            printf 'bare publish did not finish local preflight before native-vault access:\n%s\n' "$output" >&2
            exit 1
          fi
          config_root="$(mktemp -d)"
          trap 'rm -f "$source_file"; rm -r "$config_root"' EXIT
          status_output="$(XDG_CONFIG_HOME="$config_root" "$PUMD_BINARY" auth status --credential-store=file)"
          [[ "$status_output" == *"Project Authorization: not configured"* ]]
          logout_output="$(XDG_CONFIG_HOME="$config_root" "$PUMD_BINARY" auth logout --credential-store=file)"
          [[ "$logout_output" == *"credentials removed locally"* ]]
          [[ "$logout_output" == *"remote authorization may still be active"* ]]
          printf 'Hello\n' > "$source_file"
          set +e
          token_output="$(env -u PUMD_ACCESS_TOKEN "$PUMD_BINARY" publish --auth=token-env "$source_file" 2>&1)"
          token_status=$?
          set -e
          if [[ "$token_status" -ne 1 || "$token_output" != *"PUMD_ACCESS_TOKEN"* ]]; then
            printf 'native-binary ephemeral automation contract failed:\n%s\n' "$token_output" >&2
            exit 1
          fi

      - name: Prove Windows native-binary preflight is vault-free
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $sourceFile = New-TemporaryFile
          try {
            Set-Content -LiteralPath $sourceFile -Value '![unsupported](image.png)' -NoNewline
            $PSNativeCommandUseErrorActionPreference = $false
            $output = & $env:PUMD_BINARY publish $sourceFile 2>&1 | Out-String
            if ($LASTEXITCODE -ne 1 -or $output -notmatch 'rendering failed') {
              throw "bare publish did not finish local preflight before native-vault access: $output"
            }
          } finally {
            Remove-Item -LiteralPath $sourceFile -Force
          }
          $configRoot = Join-Path ([System.IO.Path]::GetTempPath()) ("pumd-native-binary-parity-" + [guid]::NewGuid())
          $paritySource = New-TemporaryFile
          New-Item -ItemType Directory -Path $configRoot | Out-Null
          try {
            $previousAppData = $env:APPDATA
            $env:APPDATA = $configRoot
            $statusOutput = & $env:PUMD_BINARY auth status --credential-store=file 2>&1 | Out-String
            if ($LASTEXITCODE -ne 0 -or $statusOutput -notmatch 'Project Authorization: not configured') {
              throw "native-binary file-store status contract failed: $statusOutput"
            }
            $logoutOutput = & $env:PUMD_BINARY auth logout --credential-store=file 2>&1 | Out-String
            if ($LASTEXITCODE -ne 0 -or $logoutOutput -notmatch 'credentials removed locally' -or $logoutOutput -notmatch 'remote authorization may still be active') {
              throw "native-binary logout contract failed: $logoutOutput"
            }
            Set-Content -LiteralPath $paritySource -Value 'Hello' -NoNewline
            Remove-Item Env:PUMD_ACCESS_TOKEN -ErrorAction SilentlyContinue
            $tokenOutput = & $env:PUMD_BINARY publish --auth=token-env $paritySource 2>&1 | Out-String
            if ($LASTEXITCODE -ne 1 -or $tokenOutput -notmatch 'PUMD_ACCESS_TOKEN') {
              throw "native-binary ephemeral automation contract failed: $tokenOutput"
            }
          } finally {
            $env:APPDATA = $previousAppData
            Remove-Item -LiteralPath $paritySource -Force
            Remove-Item -LiteralPath $configRoot -Recurse -Force
          }
          exit 0
